# Moon Cosmic Parity Issue Tracker

Last updated: 2026-03-31

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| PARITY-001 | `cosmic-text-reference/src/shape.rs` (`shape_fallback`/`shape_run`) | Advanced shaping backend is not 1:1: upstream uses `harfrust/harfbuzz`, while current implementation is on `moon_swash::shape`; complex-script substitutions/positioning may diverge on edge cases. | `DONE` | Fixed in `src/shape.mbt` by adding HarfBuzz shaping path (`moonbit-community/harfbuzz@0.1.0`) with run-relative cluster mapping and end-range alignment; font source bytes/index are wired via `src/font.mbt`. Validated with `moon info && moon fmt && moon check && moon test` (177/177 passed). |
| PARITY-002 | `cosmic-text-reference/src/shape.rs:899-965` | Missing upstream ligature-break guard around linebreak opportunities (punctuation pair probe like `|>`, `!=`, `->`) before splitting segments. | `DONE` | Fixed in `src/linebreak/linebreak.mbt` (`is_ligature_guard_pair`, `linebreaks`, `wrap_word_segments`) and routed by `src/segment.mbt`. Validated by updated boundary tests in `src/segment_test.mbt` and full `moon check && moon test` (177/177 passed). |
| PARITY-003 | `cosmic-text-reference/src/font/fallback/mod.rs` (`FontFallbackIter`) | Fallback selection pipeline is still simplified versus upstream iterator semantics (script extension timing, per-font iteration behavior, and missing-check flow). | `DONE` | Completed in `src/font.mbt` + `src/shape.mbt`: explicit iterator state (`default_i/script_i/script_family_i/common_i/other_i`), monospace lifecycle in fallback ordering, incremental per-run fallback consumption, and `check_missing` classification with configurable warning sink (`set_fallback_warning_handler` / `clear_fallback_warning_handler`). Warning text now matches upstream wording format and is locked by wbtests (`src/font_wbtest.mbt`). |
| PARITY-004 | `cosmic-text-reference/src/font/fallback/{unix,macos,windows}.rs` + target-specific selection | Platform fallback profile selection differs from upstream target-specific compilation split. | `DONE` | Scope decision: this project now targets macOS fallback behavior only for native builds. `src/font_fallback/fallback.mbt::platform_default_fallback_profile` is pinned to `MacOS` under `#cfg(target=\"native\")`, and explicit overrides (`set_fallback_profile`, `clear_fallback_profile`) remain available. |
| PARITY-005 | `cosmic-text-reference/src/shape.rs` + `unicode_linebreak::linebreaks` behavior surface | No full conformance harness against upstream `unicode_linebreak` vectors; current tests are representative but not exhaustive for all UAX#14 classes. | `DONE` | Fixed by adding upstream `LineBreakTest.txt` vector harness in `src/unicode_linebreak/unicode_linebreak_test.mbt` and porting line-break decision path to upstream-compatible pair-table state machine (`src/unicode_linebreak/pair_table.mbt` + `src/unicode_linebreak/unicode_linebreak.mbt`). Verified with `moon test -p Milky2018/moon_cosmic/unicode_linebreak --no-render` (6424 vectors, 0 mismatch). |
| PARITY-006 | `cosmic-text-reference/src/edit/vi.rs` | `edit_vi` behavior is still subset-only: no full parser/event model parity (registers/text-objects/search/motion variants) relative to upstream `modit` flow. | `IN_PROGRESS` | `src/edit_vi/vi.mbt` now includes changed/save-point/undo-redo, passthrough, `/` search input + `n/N`, line-find motions `f/F/t/T`, basic register flow (`yy/p`), text-object operator path (`diw/ciw/yiw`, bracket/quote objects), additional normal-mode commands `A/I/o/O/D/C/S/u/<C-r>`, and motions `^/gg/G`. Validated by expanded `src/edit_vi/vi_test.mbt` and full `moon fmt && moon check && moon test --no-render` (196/196 passed). Remaining gap: full `modit` parser/event parity (multi-register behavior, complete text-object/search variants, and full motion matrix). |
| PARITY-007 | `cosmic-text-reference/src/edit/syntect.rs` | `edit_syntect` currently uses an internal tokenizer, not upstream syntect grammar/state engine parity (scope stack/highlighter behavior). | `BLOCKED` | Blocked until a usable MoonBit `syntect`/equivalent dependency surface is available; per current decision this item is deferred and no further implementation work will be done before unblocked. Dependency package request filed: https://github.com/moonbit-community/Community-Tasks/issues/144 |
| PARITY-008 | `cosmic-text-reference/src/edit/mod.rs` + `src/edit/editor.rs` integration | Main package compatibility surface for `SyntaxEditor`/`ViEditor` integration differs from upstream trait-based edit layering. | `IN_PROGRESS` | Main package now exports both `ViEditor` (`src/vi.mbt`, `src/vi_test.mbt`) and `SyntaxEditor` (`src/syntax_editor.mbt`, `src/syntax_editor_test.mbt`) directly, closing most root-level edit API exposure gap. Remaining gap: full upstream-style trait-based `Edit` layering and generic editor composition path (`Editor`/`SyntaxEditor`/`ViEditor` over one shared trait abstraction). |
| PARITY-009 | MoonBit registry dependency surface | No registry package currently available for direct parity reuse of upstream parser/highlighter engines (`modit`, `syntect`). | `BLOCKED` | Verified by failed dependency resolution commands: `moon add moonbit-community/modit@0.1.0` and `moon add moonbit-community/syntect@0.1.0` (module not found in registry). Dependency package requests filed: `modit` https://github.com/moonbit-community/Community-Tasks/issues/143, `syntect` https://github.com/moonbit-community/Community-Tasks/issues/144 |
| PARITY-010 | `src/shape.mbt:759-820` vs `cosmic-text-reference/src/shape.rs:84-201` | HarfBuzz hot path lacks upstream-equivalent scratch reuse (`ShapePlan` cache + `UnicodeBuffer` reuse). Current code constructs `Face/Font/Buffer` per run, adding heavy allocation/setup overhead under mixed-script fallback workloads. | `DONE` | Fixed by caching `@font.Font` per `font_id` in `FontSystem` and reusing a shared HarfBuzz buffer (`src/font.mbt`), then switching shaping to plan-cache path `@hb_shape.shape(..., shapers=[\"ot\"])` in `src/shape.mbt`. |
| PARITY-011 | `src/font.mbt:861-918` vs `cosmic-text-reference/src/font/fallback/mod.rs:185-488` | Fallback candidate pipeline is staged but not full upstream iterator semantics (`FontFallbackIter`) with equivalent monotonic state, monospace candidate buffering, and one-pass evaluation behavior; may cause extra rescans/re-shaping in fallback loops. | `DONE` | Fixed by porting explicit fallback iterator state (`default_i/script_i/script_family_i/common_i/other_i`) and stepwise `font_fallback_next` flow in `src/font.mbt`; monospace ranking is now integrated into font fallback ordering (`font_matches_for_scripts_with_codepoints`) instead of post-processing in `shape`. |
| PARITY-012 | `src/buffer.mbt:390-425` vs `cosmic-text-reference/src/buffer.rs:909-1010` | `Buffer::set_text` rebuilds line arrays eagerly each call, while upstream aggressively reuses line/attrs allocations via reclaim/reset path; this may increase allocation churn for high-frequency updates. | `DONE` | Fixed by rewriting `Buffer::set_text` to reuse `self.lines` in-place (overwrite when possible, append only when needed, then truncate). This removes per-call line-array rebuilds and matches upstream reclaim-style flow at the buffer-line container level. |
| PARITY-013 | `cosmic-text-reference/src/buffer.rs:17-31` + `:554-569` (`DirtyFlags` + `resolve_dirty`) | Buffer dirty-state pipeline is not 1:1. Upstream tracks `RELAYOUT/TAB_SHAPE/TEXT_SET/SCROLL` and resolves lazily before shaping; current moon implementation shapes/layouts directly without equivalent dirty-flag transitions. | `DONE` | Fixed by adding explicit dirty flags in `src/buffer.mbt` (`dirty_relayout/dirty_tab_shape/dirty_text_set/dirty_scroll`), implementing `buffer_resolve_dirty`, routing `shape_until_scroll` through lazy resolve, and aligning `shape_until_cursor` with the upstream two-phase flow (`shape_until_scroll` then `SCROLL` dirty + reshape). Also applied resolve path to `layout_all*` / `shape_all_with_font_system` helper entry points. Validated with `moon info && moon fmt && moon check && moon test` (184/184 passed). |
| PARITY-014 | `cosmic-text-reference/src/buffer.rs:853-920` (`set_text_impl`) | `set_text` side-effect parity gap: upstream resets `scroll` to `Scroll::default()` after text replacement, while current moon `Buffer::set_text` keeps previous scroll state. | `DONE` | Fixed in `src/buffer.mbt` (`set_text` / `set_rich_text`) by resetting `scroll` and setting `TEXT_SET`-equivalent dirty state. Added regression test `Buffer.set_text resets scroll to default` in `src/buffer_test.mbt`. Validated with `moon info && moon fmt && moon check && moon test` (184/184 passed). |

## Current Work Queue

1. PARITY-006
2. PARITY-008
